# ╔════════════════════════════════════════════════════════════════════╗
# ║  AutoAvenue Scraper — Environment Configuration                   ║
# ╚════════════════════════════════════════════════════════════════════╝
#
#  SETUP:
#    cp .env.example .env          # copy this file
#    $EDITOR .env                  # tweak values as needed
#
#  MODES:
#    LOCAL DEV  → defaults below work out-of-the-box (SQLite, no Redis)
#    DOCKER     → uncomment PostgreSQL block, `docker compose up`
#    PRODUCTION → set real secrets, use PostgreSQL, enable HTTPS
#
# ────────────────────────────────────────────────────────────────────

# ── Database ────────────────────────────────────────────────────────
# SQLite (local dev — zero setup)
DATABASE_URL=sqlite+aiosqlite:///autoavenue.db

# PostgreSQL (uncomment for Docker / production)
# DATABASE_URL=postgresql+asyncpg://autoavenue:autoavenue_secret_2026@localhost:5533/autoavenue
# POSTGRES_USER=autoavenue
# POSTGRES_PASSWORD=autoavenue_secret_2026
# POSTGRES_DB=autoavenue

# ── Redis (optional — only needed if running Celery workers) ────────
REDIS_URL=redis://localhost:6480/0
CELERY_BROKER_URL=redis://localhost:6480/0
CELERY_RESULT_BACKEND=redis://localhost:6480/1

# ── Security ────────────────────────────────────────────────────────
# CHANGE THIS in production!
SECRET_KEY=change-me-in-production-super-secret-key-2026

# ── Scraper ─────────────────────────────────────────────────────────
# Target website (ebizautos dealer platform)
SCRAPE_BASE_URL=https://autoavenj.ebizautos.com

# Politeness delays (seconds) between page requests
SCRAPE_DELAY_MIN=2
SCRAPE_DELAY_MAX=5

# Retries per page on failure
SCRAPE_MAX_RETRIES=3

# ── Media ───────────────────────────────────────────────────────────
# Directory for downloaded vehicle photos (relative to backend/)
MEDIA_DIR=media

# ── Frontend Dev Server ─────────────────────────────────────────────
# Only used by `npm run dev` (Vite dev server proxies API calls here)
VITE_API_BASE_URL=http://localhost:8100
